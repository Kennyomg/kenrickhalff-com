/*
 * ExperienceSelector Messages
 *
 * This contains all the text for the ExperienceSelector component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ExperienceSelector';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ExperienceSelector component!',
  },
});
